export enum Mode {
  OnSet = "OnSet",
  Bpm = "Bpm",
  Pitch = "Pitch",
}

export enum Parameters {
  AttackTime = "attack",
  DecayTime = "decay",
  ReleaseTime = "release",
  SustainLevel = "sustain",
}

export enum Stage {
  Attack = "Attack",
  Decay = "Decay",
  Release = "Release",
  Sustain = "Sustain",
}
